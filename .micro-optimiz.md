# micro-optimiz ledger

## Unify failure path via `failWith` (src/assert.ts)
Goal: every assertion fails through `failWith(assertion, opts, title, block, outcome)`;
delete the 73× repeated `parseOpts` → `fail({ assertion, message: buildBlock({ assertion, title: o.msg ?? …, note: o.note }) })`.
~8–10 sites per round, each round shippable (prettier --check after). Sites using `o.actual`
as row label: `parseOpts(opts).actual ?? "actual"` inline (pure, cheap) or leave for last.
- [x] R1: add `failWith` + migrate `any`, `none`
- [ ] R2: Arrays part 1 — `all`, `one`, `count`, `len`, `longerThan`, `shorterThan`, `includes`, `first`, `last`
- [ ] R3: Arrays part 2 — `unique` … `partition`
- [ ] R4: Objects — `hasKey` … `dig`
- [ ] R5: Existence + Type guards
- [ ] R6: Equality + Numerics (many `o.actual` label sites here)
- [ ] R7: Functions + Negation
- [ ] R8: Async — fold `failResolved` / `failRejected` onto `failWith`
- [ ] R9: contract — drop dead imports (`parseOpts`/`buildBlock` if unused); delete ledger
